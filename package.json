{
  "name": "reference-overlay",
  "version": "2.0.0",
  "description": "Browser-based lower-third overlay controller for live video production",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check js/data.js && node --check js/control.js && node --check js/output.js",
    "test": "node --test",
    "install-deps": "npm install",
    "postinstall": "node scripts/postinstall-playwright.js"
  },
  "dependencies": {
    "axios": "^1.19.0",
    "cheerio": "^1.2.0",
    "pngjs": "^7.0.0",
    "playwright": "^1.62.1",
    "ws": "^8.21.1"
  },
  "overrides": {
    "undici": "^7.28.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "AGPL-3.0-only"
}
